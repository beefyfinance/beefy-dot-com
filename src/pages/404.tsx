import React, { memo } from 'react';
import { Inner } from '../components/Common/Inner';

const NotFoundPage = memo(function NotFoundPage() {
  return (
    <>
      <Inner>not found</Inner>
    </>
  );
});

export default NotFoundPage;
